[build]
  command = "npm ci && npm run build && cp -r attached_assets dist/public/"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
